machine:
  timezone: America/New_York
  node:
    version: 0.10.28

dependencies:
  pre:
    - npm install knex -g

database:
  override:
    - knex migrate:latest

test:
  override:
    - npm run-script test-cov
  post:
    - npm run-script coveralls
