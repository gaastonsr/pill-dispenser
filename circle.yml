machine:
  timezone: America/New_York
  node:
    version: 0.10.28

database:
  override:
    - psql -U ubuntu circle_test < docs/database/schema.sql

test:
  override:
    - npm run-script test-cov
  post:
    - npm run-script coveralls
